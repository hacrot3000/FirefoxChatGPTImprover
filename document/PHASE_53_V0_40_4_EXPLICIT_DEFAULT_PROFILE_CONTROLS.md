# Phase 53 v0.40.4 — Explicit default profile controls

Automation and Local action libraries now expose **Set as default**. The change affects only future fallback resolution and does not update active sessions, stopped-tab snapshots, tab overrides, drafts, downloads, or shell jobs. A default profile must be replaced explicitly before it can be deleted.
